'use client';

import VipChatContent from '@/components/vip-chat/vip-chat-content';

/**
 * VipChatPage is the main chat interface rendered inside the iframe.
 */
export default function VipChatPage() {
  return <VipChatContent />;
}
